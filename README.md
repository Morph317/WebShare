## Usage ##
### For the host: ###

Run `python server.py` to launch the server.

### For clients: ###

Get the local IP of your host, paste it into the input box at the top left corner, then click connect.
